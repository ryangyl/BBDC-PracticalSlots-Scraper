BBDC Practical Bot:

This bot requires your own telegram bot API and google gemini API.

BBDC currently has two ways of verification, one by capthca and one by login pin.

The bot will promot you if login pin via sms is required.

Captha will be solved via VLM (Gemini).
